//Jest-config.js
module.exports = {
  testMatch: ["**/*.test.js"],
  testEnvironment: "node",
  verbose: true,
  detectOpenHandles: true, // Specify the pattern for test files
  // Add any other Jest configuration options as needed
};
